<!DOCTYPE html>
  <body>
    <h2> <%= @band.name %> </h2>
    <table>
    <th><h3>Discography</h3></th>
    <% @band.albums.each do |album| %>
      <tr> <td><%= link_to album.title, album_url(album)%> </td> <tr>
      <% end %>
  </body>
